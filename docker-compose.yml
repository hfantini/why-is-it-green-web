services:
  web:
    container_name: why-is-it-green-web
    build:
      context: .
      dockerfile: Dockerfile
      args:
        ENVIRONMENT: development
        BUILD_NUMBER: 1
        GIT_SHA: 7f3c9ab
    ports:
      - '8080:80'
    restart: unless-stopped
